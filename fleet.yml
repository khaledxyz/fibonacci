defaultNamespace: default
targets:
  - name: production
    clusterSelector:
      matchLabels:
        environment: production
    paths:
      - ./.k8s
  - name: development
    clusterSelector:
      matchLabels:
        environment: development
    paths:
      - ./.k8s
