defaultNamespace: default
targets:
  - name: production
    clusterSelector:
      matchLabels:
        environment: production
  - name: development
    clusterSelector:
      matchLabels:
        environment: development
